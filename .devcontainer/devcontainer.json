{
  "updateContentCommand": "corepack enable && corepack prepare yarn@1.22.22 --activate && yarn --frozen-lockfile",
  "postAttachCommand": "yarn start",
  "customizations": {
    "codespaces": {
      "openFiles": ["src/App.js"]
    }
  }
}
